import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#000',
  },
  title: {
    marginBottom: '5%',
    fontSize: 26,
    textAlign: 'center',
    fontWeight: 'bold',
    color: '#fff',
  },
  flatListContainer: {
    marginHorizontal: '2%',
  },
  buttonContainer: {
    margin: '3%',
  },
});

export default styles;
