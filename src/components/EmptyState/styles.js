import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignSelf: 'center',
    width: '50%',
  },
  image: {
    width: '100%',
    height: '100%',
  },
});

export default styles;
